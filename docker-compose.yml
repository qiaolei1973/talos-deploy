services:
  portal:
    build: ./server
    ports:
      - "${PORTAL_PORT:-3080}:8080"
    environment:
      - JWT_SECRET=dev-secret-change-me
      - SANDBOX_MANAGER_URL=http://sandbox-manager:8081
      - NEWAPI_BASE=http://new-api:3000
      - NEWAPI_ROOT_PASSWORD=12345678
      - ADMIN_USERNAME=admin
      - ADMIN_PASSWORD=admin
      - SANDBOX_NAMESPACE=sandbox-workspaces
      - SANDBOX_TEMPLATE=claude-workspace
      - UPSTREAM_API_KEY=test
      - UPSTREAM_BASE_URL=https://test.example.com
    volumes:
      - portal-data:/app/data
    depends_on:
      - sandbox-manager
      - new-api

  sandbox-manager:
    build: ./sandbox-manager
    environment:
      - SANDBOX_MODE=mock
      - SANDBOX_NAMESPACE=sandbox-workspaces
      - PORT=8081
    ports:
      - "8081:8081"

  new-api:
    image: calciumion/new-api:latest
    ports:
      - "${NEWAPI_PORT:-3081}:3000"
    volumes:
      - new-api-data:/data

volumes:
  portal-data:
  new-api-data:
