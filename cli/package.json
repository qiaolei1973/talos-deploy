{
  "name": "talos-deploy",
  "version": "0.1.0",
  "description": "Talos Deploy CLI — sandbox environments for Claude Code",
  "type": "module",
  "bin": {
    "talosd": "./dist/index.js"
  },
  "files": [
    "dist/"
  ],
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc && chmod +x dist/index.js",
    "prepublishOnly": "npm run build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ob-labs/talos-deploy"
  },
  "license": "MIT",
  "dependencies": {
    "commander": "^12.0.0",
    "dotenv": "^17.4.2",
    "eventsource": "^4.1.0",
    "ws": "^8.21.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "@types/ws": "^8.18.1",
    "tsx": "^4.0.0",
    "typescript": "^5.5.0",
    "vitest": "^3.2.6"
  }
}
